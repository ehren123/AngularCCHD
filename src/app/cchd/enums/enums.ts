// Result of entire test.
export enum TestResultEnum{
    error,
    inProgress,
    pass,
    refer
}

// Generic Result.
export enum ResultEnum{
    error,
    fail,
    inconclusive,
    pass
}

// Iteration status.
export enum IterationStatusEnum{
    error,
    pass,
    fail,
    inconclusive,
    inProgress
}

